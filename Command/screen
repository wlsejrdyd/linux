시간이 오래걸리는 명령어를 실행시켰는데 세션이 계속 종료된다면, PC 에서 세션이 종료되어도 명령어가 계속 동작될 수 있게 끔 screen 명령어를 통해 끊임없이 작업진행이 가능하다

아래에있는 방법이 내가 제일 많이 쓰는방법

# screen -S jdy     (스크린 생성)
명령어 명령어 명령어~~~~
# ctrl + a + d (exit)     (스크린 나가기)
# screen -list     (켜져있는 스크린 보기)
# screen -r jdy (스크린 재접속)
# exit
